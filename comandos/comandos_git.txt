git init .           -> Isso cria um repo git no diretório atual
git status           -> Mostra o status do tarabalho (de cada arquivo)
git add nome_arquivo -> Adiciona arquivo para ser commitado
git commit -m "msg"  -> Faz commit do que estava para ser adicionado no repo
git log              -> Mostra todo o histórico de commit 
